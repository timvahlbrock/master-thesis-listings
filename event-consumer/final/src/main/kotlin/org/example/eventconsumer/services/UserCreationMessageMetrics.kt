package org.example.eventconsumer.services

import org.springframework.stereotype.Service

@Service
class UserCreationMessageMetrics {
	fun unroutableErrors(level: String?, message: String?) {
		TODO("Not needed to demonstrate contract tests")
	}
}